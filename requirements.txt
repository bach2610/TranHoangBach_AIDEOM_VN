# =====================================================================
# AIDEOM-VN — môi trường chạy ứng dụng Streamlit (Bài 1 -> 12)
# Cài đặt:  pip install -r requirements.txt
# =====================================================================

# --- Khoa học dữ liệu cơ bản ---
numpy>=1.24
pandas>=2.0
scipy>=1.10

# --- Giao diện & trực quan hóa ---
streamlit>=1.28
plotly>=5.17

# --- Tối ưu hóa tuyến tính / nguyên / phi tuyến ---
pulp>=2.7
cvxpy>=1.4
pyomo>=6.6

# --- Tối ưu đa mục tiêu (NSGA-II) ---
pymoo>=0.6.1

# --- Học tăng cường (Bài 11) ---
gymnasium>=0.29

# --- Kiểm thử ---
pytest>=7.4
